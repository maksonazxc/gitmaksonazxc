# 📧 Налаштування Email Повідомлень

## 🎯 Що реалізовано

Система автоматично відправляє повідомлення на **svynoriz56@gmail.com** при кожній новій реєстрації користувача.

## 🔧 Варіанти реалізації

### 1. **Локальна імітація (поточна)**
- Email дані зберігаються в `localStorage` браузера
- Можна переглянути в консолі розробника
- Підходить для тестування без сервера

### 2. **PHP + локальний сервер**
- Файл `send_email.php` готовий до використання
- Потрібен локальний сервер (XAMPP, WAMP, MAMP)
- Використовує PHP `mail()` функцію

### 3. **Сервіси email (рекомендовано)**

#### EmailJS (безкоштовно)
```javascript
// Додати в register.html
emailjs.send("service_id", "template_id", {
  to_email: "svynoriz56@gmail.com",
  user_name: userData.name,
  user_email: userData.email,
  message: userData.msg
});
```

#### Formspree (безкоштовно)
```html
<form action="https://formspree.io/f/YOUR_FORM_ID" method="POST">
```

## 🚀 Швидке налаштування EmailJS

1. **Реєстрація:**
   - Йдіть на https://emailjs.com
   - Створіть безкоштовний акаунт

2. **Налаштування:**
   - Додайте email сервіс (Gmail)
   - Створіть email шаблон
   - Отримайте Service ID та Template ID

3. **Інтеграція:**
   ```html
   <!-- Додати в index.html -->
   <script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>
   ```

4. **Код для register.html:**
   ```javascript
   // Замінити функцію sendEmailNotification
   function sendEmailNotification(userData) {
     emailjs.send("YOUR_SERVICE_ID", "YOUR_TEMPLATE_ID", {
       to_email: "svynoriz56@gmail.com",
       user_name: userData.name,
       user_email: userData.email,
       user_message: userData.msg || "Не вказано",
       registration_date: new Date(userData.registeredAt).toLocaleString('uk-UA')
     }).then(() => {
       console.log('✅ Email відправлено успішно!');
     }).catch((error) => {
       console.error('❌ Помилка відправки email:', error);
     });
   }
   ```

## 📊 Моніторинг

### Перевірка відправлених email:
1. Відкрийте консоль браузера (F12)
2. Перейдіть на вкладку "Application" → "Local Storage"
3. Знайдіть ключ `emailQueue`

### Лог файли (для PHP):
- `email_log.txt` - успішні відправки
- `email_errors.txt` - помилки

## 🔐 Безпека

- Ніколи не зберігайте API ключі в коді
- Використовуйте змінні середовища
- Обмежте домени для EmailJS

## 📞 Підтримка

Якщо потрібна допомога з налаштуванням - звертайтесь!
